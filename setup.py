from setuptools import setup, find_packages

setup(
    name="arabic-voc-platform",
    version="1.0.0",
    description="Arabic Voice of Customer Platform",
    packages=find_packages(),
    python_requires=">=3.11",
)